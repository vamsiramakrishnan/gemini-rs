use crate::app::AppRegistry;

// Apps will be added by subsequent tasks (CUI-4, CUI-6, CUI-7).
// For now, register nothing (empty hub).

pub fn register_all(_registry: &mut AppRegistry) {
    // Will be filled in by CUI-4, CUI-6, CUI-7
}
