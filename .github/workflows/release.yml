name: Release

# Triggered by pushing a vX.Y.Z tag (created by scripts/release.sh).
# Flow: validate → publish (L0→L1→L2→server→cli) → GitHub Release
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-*"   # pre-releases: v0.6.0-rc.1

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

permissions:
  contents: write   # needed to create GitHub Release

# ─────────────────────────────────────────────────────────────────────────────
# Job 1 — Validate: tests + clippy (same gating as CI, but on the tagged commit)
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93.1"
          components: clippy, rustfmt
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all -- --check
      - run: cargo test --workspace
      - run: cargo clippy --workspace --all-targets -- -D warnings

# ─────────────────────────────────────────────────────────────────────────────
# Job 2 — Publish: crates.io in strict dependency order
#
# Publish order (must match dependency graph):
#   L0  rs-genai
#   L1  rs-adk          (depends on rs-genai)
#   L2  adk-rs-fluent   (depends on rs-genai, rs-adk)
#   app adk-server-core (depends on rs-adk)
#   bin adk-cli         (depends on rs-genai, rs-adk, adk-rs-fluent, adk-server-core)
#
# adk-web and adk-api-server are publish = false (deployment apps, not library crates).
#
# Each step:
#   1. Reads version from Cargo metadata (single source of truth: workspace version)
#   2. Checks crates.io API — skips if that exact version already exists
#   3. Polls until the crate appears in the sparse index (max ~3 min) before
#      proceeding to the next crate, so downstream crates can resolve the dep
# ─────────────────────────────────────────────────────────────────────────────
  publish:
    name: Publish to crates.io
    needs: validate
    runs-on: ubuntu-latest
    environment: crates-io
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.93.1"
      - uses: Swatinem/rust-cache@v2

      # ── Helper: publish one crate, then poll until it appears in the index ──
      # Usage:  publish_and_wait <crate-name>
      - name: Define publish helper
        run: |
          cat > /tmp/publish.sh << 'HELPER'
          #!/usr/bin/env bash
          set -euo pipefail
          CRATE=$1
          VERSION=$(cargo metadata --format-version=1 --no-deps | \
            jq -r --arg c "$CRATE" '.packages[] | select(.name == $c) | .version')

          echo "::group::$CRATE @ $VERSION"

          # Check if already published
          HTTP=$(curl -sI -o /dev/null -w "%{http_code}" \
            "https://static.crates.io/index/$CRATE/$CRATE-${VERSION}.crate" 2>/dev/null || echo 000)
          if [ "$HTTP" = "200" ]; then
            echo "✓ $CRATE@$VERSION already on crates.io — skipping"
            echo "::endgroup::"
            exit 0
          fi

          echo "Publishing $CRATE@$VERSION ..."
          cargo publish -p "$CRATE" --no-verify

          # Poll sparse index until the crate appears (max 180 s)
          echo "Waiting for $CRATE@$VERSION to appear in the sparse index..."
          for i in $(seq 1 36); do
            sleep 5
            HTTP=$(curl -sI -o /dev/null -w "%{http_code}" \
              "https://static.crates.io/index/$CRATE/$CRATE-${VERSION}.crate" 2>/dev/null || echo 000)
            if [ "$HTTP" = "200" ]; then
              echo "✓ $CRATE@$VERSION confirmed in index (${i}×5 s)"
              echo "::endgroup::"
              exit 0
            fi
            echo "  …still waiting ($((i * 5)) s elapsed)"
          done
          echo "::warning::Timed out waiting for $CRATE@$VERSION in sparse index after 180 s"
          echo "::endgroup::"
          HELPER
          chmod +x /tmp/publish.sh

      - name: Publish rs-genai (L0)
        run: /tmp/publish.sh rs-genai
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Publish rs-adk (L1)
        run: /tmp/publish.sh rs-adk
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Publish adk-rs-fluent (L2)
        run: /tmp/publish.sh adk-rs-fluent
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Publish adk-server-core
        run: /tmp/publish.sh adk-server-core
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Publish adk-cli
        run: /tmp/publish.sh adk-cli
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

# ─────────────────────────────────────────────────────────────────────────────
# Job 3 — GitHub Release: uses the pre-written GITHUB_RELEASE_vX.Y.Z.md file
#          if it exists; falls back to auto-generated changelog
# ─────────────────────────────────────────────────────────────────────────────
  github-release:
    name: Create GitHub Release
    needs: publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Extract version from tag
        id: ver
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build release body
        id: body
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          NOTES_FILE="GITHUB_RELEASE_v${VERSION}.md"

          if [ -f "$NOTES_FILE" ]; then
            echo "Using pre-written release notes: $NOTES_FILE"
            {
              echo "body<<RELEASE_EOF"
              cat "$NOTES_FILE"
              echo "RELEASE_EOF"
            } >> "$GITHUB_OUTPUT"
          else
            echo "No $NOTES_FILE found — generating from git log"
            PREV_TAG=$(git tag --sort=-version:refname | grep -v "^${GITHUB_REF_NAME}$" | head -1)
            if [ -n "$PREV_TAG" ]; then
              RANGE="${PREV_TAG}..${GITHUB_REF_NAME}"
            else
              RANGE="${GITHUB_REF_NAME}"
            fi
            {
              echo "body<<RELEASE_EOF"
              echo "## What's Changed"
              echo ""
              git log --oneline --no-decorate "$RANGE" | while read -r line; do
                echo "- $line"
              done
              echo ""
              echo "## Crates"
              echo ""
              echo "| Crate | Version |"
              echo "|-------|---------|"
              echo "| \`rs-genai\` | $VERSION |"
              echo "| \`rs-adk\` | $VERSION |"
              echo "| \`adk-rs-fluent\` | $VERSION |"
              echo "| \`adk-server-core\` | $VERSION |"
              echo "| \`adk-cli\` | $VERSION |"
              echo "RELEASE_EOF"
            } >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "v${{ steps.ver.outputs.version }}"
          body: ${{ steps.body.outputs.body }}
          draft: false
          prerelease: ${{ contains(steps.ver.outputs.version, '-') }}
          make_latest: ${{ !contains(steps.ver.outputs.version, '-') }}
