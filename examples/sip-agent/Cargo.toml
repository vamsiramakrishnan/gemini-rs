[package]
name = "example-sip-agent"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
gemini-adk-fluent-rs = { workspace = true, features = ["sip"] }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
dotenvy = "0.15"

[lints]
workspace = true
