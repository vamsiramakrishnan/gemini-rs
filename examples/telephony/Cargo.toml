[package]
name = "example-telephony"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
gemini-adk-fluent-rs = { workspace = true }
tokio = { version = "1", features = ["full"] }
axum = { version = "0.8", features = ["ws"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
dotenvy = "0.15"

[lints]
workspace = true
